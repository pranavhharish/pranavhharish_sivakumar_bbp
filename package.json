{
  "name": "fermentation-data-platform",
  "version": "1.0.0",
  "description": "Full-stack fermentation data visualization platform",
  "private": true,
  "author": "Pranavh",
  "license": "MIT",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../database && npm install && cd ..",
    "dev": "echo 'Start backend: cd backend && npm run dev' && echo 'Start frontend: cd frontend && npm run dev'",
    "build:all": "cd backend && npm run build && cd ../frontend && npm run build && cd ..",
    "test:backend": "cd backend && npm test",
    "lint:all": "cd backend && npm run lint && cd ../frontend && npm run lint"
  },
  "workspaces": [
    "backend",
    "frontend",
    "database"
  ],
  "keywords": [
    "fermentation",
    "bioprocess",
    "data-visualization",
    "csv-upload"
  ]
}
